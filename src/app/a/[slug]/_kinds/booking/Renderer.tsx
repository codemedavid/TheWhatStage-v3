import { GenericComingSoon } from '../shared/GenericComingSoon'
import type { KindRendererProps } from '../types'

export default function BookingRenderer(props: KindRendererProps) {
  // Stub — replaced by the Booking kind PR.
  return <GenericComingSoon kindLabel="Booking" {...props} />
}
