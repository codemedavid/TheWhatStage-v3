import { GenericComingSoon } from '../shared/GenericComingSoon'
import type { KindRendererProps } from '../types'

export default function FormRenderer(props: KindRendererProps) {
  // Stub — replaced by the Form kind PR.
  return <GenericComingSoon kindLabel="Form" {...props} />
}
