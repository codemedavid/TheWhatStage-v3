import { GenericComingSoon } from '../shared/GenericComingSoon'
import type { KindRendererProps } from '../types'

export default function QualificationRenderer(props: KindRendererProps) {
  // Stub — replaced by the Qualification kind PR.
  return <GenericComingSoon kindLabel="Qualification" {...props} />
}
