// Stub for the 'server-only' Next.js package.
// In production Next.js throws if this module is imported client-side.
// In Vitest (jsdom) we just no-op so server-side modules can be unit-tested.
export {}
